DEEPSEEK_API_KEY=
SUPABASE_URL=
SUPABASE_KEY=
# ^ service_role key, NOT anon — RLS is enabled with no policies, so anon
# can't read or write anything. See README "Admin login" / security section.
SUPABASE_PROJECT_ID=
DISCORD_WEBHOOK_URL=
